apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-intelligence-service
  namespace: resolveops-dev
  labels:
    app: github-intelligence-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: github-intelligence-service
  template:
    metadata:
      labels:
        app: github-intelligence-service

    spec:

      containers:
      - name: github-intelligence-service
        image: ${ACR_LOGIN_SERVER}/github-intelligence-service:${GITHUB_SHA}
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: resolveops-config
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          runAsNonRoot: true
          allowPrivilegeEscalation: false
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: github-intelligence-service
  namespace: resolveops-dev
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: github-intelligence-service
