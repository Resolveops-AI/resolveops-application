apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway-service
  labels:
    app: api-gateway-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway-service
  template:
    metadata:
      labels:
        app: api-gateway-service
      annotations:
        azure.workload.identity/use: "true"
    spec:
      nodeSelector:
        kubernetes.azure.com/mode: user
      serviceAccountName: resolveops-workload-identity-sa
      securityContext:
        fsGroup: 10001
      containers:
      - name: api-gateway-service
        image: resolveopsacr05.azurecr.io/api-gateway-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: resolveops-config
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: resolveops-secrets
              key: DATABASE_URL
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: resolveops-secrets
              key: JWT_SECRET
        - name: AZURE_CLIENT_ID
          value: "ab0e6d5f-bff8-4046-acb3-250739f98ae8"
        - name: AZURE_TENANT_ID
          value: "81c422d3-b9ef-49b9-b8a9-b10a5f4ce484"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          runAsNonRoot: true
          runAsUser: 10001
          runAsGroup: 10001
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        livenessProbe:
          httpGet:
            path: /openapi.json
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /openapi.json
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        volumeMounts:
        - name: secrets-store-inline
          mountPath: "/mnt/secrets-store"
          readOnly: true
      volumes:
      - name: secrets-store-inline
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "azure-kvname-workload-identity-db"
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: api-gateway-service
