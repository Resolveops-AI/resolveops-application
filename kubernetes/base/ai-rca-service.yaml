apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-rca-service
  labels:
    app: ai-rca-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-rca-service
  template:
    metadata:
      labels:
        app: ai-rca-service
      annotations:
        azure.workload.identity/use: "true"
    spec:
      nodeSelector:
        kubernetes.azure.com/mode: user
      serviceAccountName: resolveops-workload-identity-sa
      securityContext:
        fsGroup: 10001
      containers:
      - name: ai-rca-service
        image: resolveopsacr05.azurecr.io/ai-rca-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: resolveops-config
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          runAsNonRoot: true
          runAsUser: 10001
          runAsGroup: 10001
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        volumeMounts:
        - name: secrets-store-inline
          mountPath: "/mnt/secrets-store"
          readOnly: true
      volumes:
      - name: secrets-store-inline
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "azure-kvname-workload-identity"
---
apiVersion: v1
kind: Service
metadata:
  name: ai-rca-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: ai-rca-service
