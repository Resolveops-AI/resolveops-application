apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: resolveops-prod

resources:
- ../../base
- namespace.yaml

patches:
- path: configmap-patch.yaml
  target:
    kind: ConfigMap
    name: resolveops-config
- path: replica-patch.yaml
