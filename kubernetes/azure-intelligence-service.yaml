apiVersion: apps/v1
kind: Deployment
metadata:
  name: azure-intelligence-service
  namespace: resolveops-dev
  labels:
    app: azure-intelligence-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: azure-intelligence-service
  template:
    metadata:
      labels:
        app: azure-intelligence-service
      annotations:
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: resolveops-workload-identity-sa
      containers:
      - name: azure-intelligence-service
        image: ${ACR_LOGIN_SERVER}/azure-intelligence-service:${GITHUB_SHA}
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: resolveops-config
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          runAsNonRoot: true
          allowPrivilegeEscalation: false
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        volumeMounts:
        - name: secrets-store-inline
          mountPath: "/mnt/secrets-store"
          readOnly: true

      volumes:
      - name: secrets-store-inline
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "azure-kvname-workload-identity"
---
apiVersion: v1
kind: Service
metadata:
  name: azure-intelligence-service
  namespace: resolveops-dev
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: azure-intelligence-service
